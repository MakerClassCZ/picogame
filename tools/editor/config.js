// The ONE file that differs between the two copies of this editor -- everything else is a
// byte-identical mirror (tools/sync.py, group EDITOR, keeps them that way).
//
// Standalone copy: served from a clone (python3 -m http.server) or any static host, with no
// local playground, so "Try in playground" opens the hosted one.
window.PG_PLAYGROUND_URL = "https://picogame.makerclass.cz/playground/";
