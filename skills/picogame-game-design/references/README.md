# references/

Pull a file only when a step in `../SKILL.md` points to it. What each one is — and when to read it —
lives in **`../SKILL.md` Part 2** (the single source, so it can't drift). For `genre-patterns.md`,
read only your genre's section, not the whole file.
